name: m169-abschlussprojekt

services:
  mediawiki-db:
    image: mariadb:10.11
    container_name: m169-mediawiki-db
    restart: unless-stopped
    environment:
      MYSQL_DATABASE: mediawiki
      MYSQL_USER: mediawiki
      MYSQL_PASSWORD: ${MEDIAWIKI_DB_PASSWORD:?Set MEDIAWIKI_DB_PASSWORD in compose/.env}
      MYSQL_RANDOM_ROOT_PASSWORD: "yes"
    volumes:
      - mediawiki_db:/var/lib/mysql
    networks:
      - backend
    mem_limit: 768m
    cpus: "1.00"

  mediawiki:
    image: mediawiki:latest
    container_name: m169-mediawiki
    restart: unless-stopped
    depends_on:
      - mediawiki-db
    ports:
      - "${MEDIAWIKI_PORT:-8085}:80"
    volumes:
      - mediawiki_data:/var/www/html
    networks:
      - frontend
      - backend
    mem_limit: 768m
    cpus: "1.00"

  nextcloud-db:
    image: mariadb:10.11
    container_name: m169-nextcloud-db
    restart: unless-stopped
    command: --transaction-isolation=READ-COMMITTED --binlog-format=ROW
    environment:
      MYSQL_DATABASE: nextcloud
      MYSQL_USER: nextcloud
      MYSQL_PASSWORD: ${NEXTCLOUD_DB_PASSWORD:?Set NEXTCLOUD_DB_PASSWORD in compose/.env}
      MYSQL_ROOT_PASSWORD: ${NEXTCLOUD_DB_ROOT_PASSWORD:?Set NEXTCLOUD_DB_ROOT_PASSWORD in compose/.env}
    volumes:
      - nextcloud_db:/var/lib/mysql
    networks:
      - backend
    mem_limit: 1024m
    cpus: "1.50"

  nextcloud-redis:
    image: redis:7-alpine
    container_name: m169-nextcloud-redis
    restart: unless-stopped
    networks:
      - backend
    mem_limit: 256m
    cpus: "0.50"

  nextcloud:
    image: nextcloud:apache
    container_name: m169-nextcloud
    restart: unless-stopped
    depends_on:
      - nextcloud-db
      - nextcloud-redis
    ports:
      - "${NEXTCLOUD_PORT:-8080}:80"
    environment:
      MYSQL_HOST: nextcloud-db
      MYSQL_DATABASE: nextcloud
      MYSQL_USER: nextcloud
      MYSQL_PASSWORD: ${NEXTCLOUD_DB_PASSWORD:?Set NEXTCLOUD_DB_PASSWORD in compose/.env}
      REDIS_HOST: nextcloud-redis
    volumes:
      - nextcloud_data:/var/www/html
    networks:
      - frontend
      - backend
    mem_limit: 2048m
    cpus: "2.00"

  nextcloud-cron:
    image: nextcloud:apache
    container_name: m169-nextcloud-cron
    restart: unless-stopped
    depends_on:
      - nextcloud
    entrypoint: /cron.sh
    volumes:
      - nextcloud_data:/var/www/html
    networks:
      - backend
    mem_limit: 256m
    cpus: "0.50"

  gitea-db:
    image: postgres:16-alpine
    container_name: m169-gitea-db
    restart: unless-stopped
    environment:
      POSTGRES_DB: gitea
      POSTGRES_USER: gitea
      POSTGRES_PASSWORD: ${GITEA_DB_PASSWORD:?Set GITEA_DB_PASSWORD in compose/.env}
    volumes:
      - gitea_db:/var/lib/postgresql/data
    networks:
      - backend
    mem_limit: 768m
    cpus: "1.00"

  gitea:
    image: gitea/gitea:latest
    container_name: m169-gitea
    restart: unless-stopped
    depends_on:
      - gitea-db
    environment:
      GITEA__database__DB_TYPE: postgres
      GITEA__database__HOST: gitea-db:5432
      GITEA__database__NAME: gitea
      GITEA__database__USER: gitea
      GITEA__database__PASSWD: ${GITEA_DB_PASSWORD:?Set GITEA_DB_PASSWORD in compose/.env}
    ports:
      - "${GITEA_WEB_PORT:-3000}:3000"
      - "${GITEA_SSH_PORT:-2222}:22"
    volumes:
      - gitea_data:/data
    networks:
      - frontend
      - backend
    mem_limit: 768m
    cpus: "1.00"

  portainer:
    image: portainer/portainer-ce:latest
    container_name: m169-portainer
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    ports:
      - "${PORTAINER_HTTPS_PORT:-9443}:9443"
      - "${PORTAINER_HTTP_PORT:-9000}:9000"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - portainer_data:/data
    networks:
      - frontend
    mem_limit: 256m
    cpus: "0.50"

networks:
  frontend:
    name: m169_frontend
  backend:
    name: m169_backend
    internal: true

volumes:
  # Portainer
  portainer_data:

  # Gitea ("Gitty")
  gitea_data:
  gitea_db:

  # Nextcloud
  nextcloud_data:
  nextcloud_db:

  # MediaWiki
  mediawiki_data:
  mediawiki_db:
